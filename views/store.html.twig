{% include 'top.html.twig' %}
  <h1> {{ store.getName }} </h1>
  <h2> {{ store.getCity }} </h2>
  {% if brands is not empty %}
  {% for brander in brands %}
  <ul>
    <li><a href="/brand{{ brander.getId }}">{{ brander.getName}}</a> {{ brander.getType }}</p>
  </ul>
  {% endfor %}
  {% else %}
  <h3> This store does not seem to carry any brands yet :(</h3>
  {% endif %}
  <h3> Add Brand </h3>
  <form action="/newbrand" method="post">
    <select class="form-control" name="brand">
    {% for abrand in allbrands %}
      <option value="{{ abrand.getId }}">{{ abrand.getName }}</option>
    {% endfor %}
  </select>
    <button type="submit" value="{{store.getId}}" name="id">Add</button>
</form>
<h3> Update {{store.getName}}</h3>
  <form action="/store/{{store.getId}}" method="post">
    <input name= "_method" type="hidden" value="patch">
    <label for="newname">Rename Store:</label>
    <input id="newname" name="newname" type="text">
    <button type="submit">Update</button>
  </form>
{% include 'bottom.html.twig'%}
